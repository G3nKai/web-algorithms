//(DB, distFunc, Q, epsilon) DB===points[] Q===dot  dist===euclidianDistance epsilon===задается на сайте пользователем
function RangeQuery(dot) {
    let neighbours = [];
    for (let i = 0, len = points.length; i < len; i++) {
        
    }
}